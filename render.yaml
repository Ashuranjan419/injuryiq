services:
  - type: web
    name: injuryiq
    env: python
    runtime: python
    region: oregon
    plan: free
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: gunicorn app:app
      - key: FLASK_DEBUG
        value: False
      - key: SECRET_KEY
        sync: false
      - key: MONGO_URI
        sync: false
